export type ThemeMode = 'light' | 'dark';

export interface ThemeColors {
  bg: string;
  bgSecondary: string;
  bgTertiary: string;
  text: string;
  textDim: string;
  accent: string;
  accentDim: string;
  border: string;
}